---
phase: 6
plan: 1
wave: 1
---

# Plan 6.1: Hero Visibility & Transparent Sticker Branding

## Objective
Fix the Hero section heading character clipping (letters cut off at the top by the navbar overlap) and add a stylish, high-contrast Neo-Brutalist transparent PNG sticker/icon overlay in the heading.

## Context
- .gsd/SPEC.md
- App.tsx
- components/InteractiveText.tsx

## Tasks

<task type="auto">
  <name>Generate Transparent PNG Sticker Asset</name>
  <files>public/stickers/neon_star.png</files>
  <action>
    - Create a directory `public/stickers` if it does not exist.
    - Generate a high-contrast transparent PNG star or badge design sticker using the image generation tool.
    - Prompt: "Neo-Brutalist style high-resolution transparent PNG sticker of a bright neon green star with a thick black outline, vector art style, isolated on transparent background, no shadow, clean edges."
  </action>
  <verify>Check that the file exists and is high quality.</verify>
  <done>Sticker asset is generated and saved under `public/stickers/neon_star.png`.</done>
</task>

<task type="auto">
  <name>Fix Hero Padding & Line Height</name>
  <files>App.tsx</files>
  <action>
    - Change Hero `<section>` vertical padding from `px-4` to include top and bottom padding (`pt-36 pb-16 px-4`) to push the main Hero Box down, completely avoiding any overlap with the floating fixed navbar.
    - Change the main Hero `<section>` `min-h-[85vh]` to `min-h-[95vh]` or `min-h-screen` so that the layout feels incredibly grand yet perfectly spaced.
    - Change the Hero `h1` `leading-[0.85]` to `leading-none` or `leading-[1.0]` to prevent rendering-level clipping of letter ascenders.
  </action>
  <verify>Inspect the Hero section in the browser to ensure no letters are cut off at the top and the navbar sits cleanly above the box.</verify>
  <done>All characters in "ELEVATE YOUR STYLE GAME" are fully visible and perfectly positioned.</done>
</task>

<task type="auto">
  <name>Integrate Sticker Asset in Heading</name>
  <files>App.tsx</files>
  <action>
    - Import the transparent PNG star sticker in the Hero heading area.
    - Position it absolutely within the `h1` container, rotated slightly (e.g., `rotate-[15deg]`), overlapping the text subtly or resting as a badge in the corner.
    - Add a hover micro-animation to the sticker (e.g., spinning slightly or scaling) using Framer Motion.
  </action>
  <verify>Hover over the sticker inside the hero heading to see the animation.</verify>
  <done>The star sticker is beautifully integrated into the hero layout with a high-end interactive feel.</done>
</task>

## Success Criteria
- [ ] Hero text "ELEVATE YOUR STYLE GAME" is 100% visible, even on small screens, and never covered by the navbar.
- [ ] Letters of the heading have their top parts intact (no flat cuts).
- [ ] A stunning neon green star sticker is absolutely positioned within the heading and has hover micro-animations.
