---
phase: 5
plan: 2
wave: 1
---

# Plan 5.2: Glitch Animations & Cinematic Interactivity

## Objective
Implement high-end glitch animations for headings that trigger on scroll and hover, enhancing the "cinematic" Neo-Brutalist brand identity.

## Context
- .gsd/SPEC.md
- components/GlitchText.tsx
- components/SectionHeader.tsx
- components/InteractiveText.tsx

## Tasks

<task type="auto">
  <name>Enhance GlitchText Animation</name>
  <files>components/GlitchText.tsx</files>
  <action>
    - Refine the `glitch1` variants to use snappier timing (duration 0.1s or 0.15s).
    - Add a `forceGlitch` prop to trigger the effect programmatically.
    - Improve the color offset spans to use `mix-blend-mode` for a more premium visual effect (e.g., `difference` or `screen`).
    - Add a slight "scanline" or horizontal slice effect during the glitch.
  </action>
  <verify>Hover over GlitchText elements to see the improved animation quality.</verify>
  <done>Glitch animation feels snappy, high-quality, and "cinematic".</done>
</task>

<task type="auto">
  <name>Integrate Glitch into Section Headers</name>
  <files>components/SectionHeader.tsx</files>
  <action>
    - Update `SectionHeader` to use `GlitchText` for its title.
    - Implement a scroll-triggered "entrance glitch" that fires once when the header enters the viewport.
    - Combine `GlitchText` with the existing `InteractiveText` if possible, or swap them depending on the design impact (glitch is priority for this phase).
    - Ensure the "Protocol" badge also has a subtle entrance glitch.
  </action>
  <verify>Scroll down the page and observe the section headers glitching into view.</verify>
  <done>All section headers have a consistent, high-impact interactive entry.</done>
</task>

## Success Criteria
- [ ] Glitch effect is visually superior and snappy.
- [ ] Section headers automatically glitch when scrolled into view.
- [ ] Interactive feel of the site is significantly enhanced without performance lag.
